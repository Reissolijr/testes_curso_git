import React from 'react';
import { View, Text } from 'react-native';

const OrcamentoScreen = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Tela de Orçamento</Text>
        </View>
    );
};

export default OrcamentoScreen;
